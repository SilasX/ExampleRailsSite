<%= form_for(@argument_node) do |f| %>
  <% if @argument_node.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@argument_node.errors.count, "error") %> prohibited this argument_node from being saved:</h2>

      <ul>
      <% @argument_node.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

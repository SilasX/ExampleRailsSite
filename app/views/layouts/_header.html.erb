<div class = "header bar button">
	This is where the header will appear.<br>
	<%= if user_signed_in? %>
		<% link_to "Sign Out" destroy_user_session_path %>
	<%= else %>
		<% link_to "Sign In" new_user_session_path %>
	<%= end %>
</div>